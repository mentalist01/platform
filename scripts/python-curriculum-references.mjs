// Independent solutions used by the curriculum audit, never by answer grading.
const labelled = (...labels) => labels.map(label => `print(${JSON.stringify(label)}, input())`).join('\n');
const swap = 'a = input()\nb = input()\na, b = b, a\nprint(a)\nprint(b)';
export const references = {
  101: {
    'Первая программа': 'print("Привет, Python!")',
    'Три строки': 'print("ввод\\nобработка\\nвывод")',
    'Подписи и значения': 'print("Язык: Python\\nВерсия: 3")',
    'Повторить ввод': 'print(input())',
    'Вежливое приветствие': 'print(f"Привет, {input()}!")',
    'Фамилия перед именем': 'name = input()\nsurname = input()\nprint(surname, name)',
    'Следующее и предыдущее': 'n = int(input())\nprint(n+1)\nprint(n-1)',
    'Сумма и произведение': 'a = int(input())\nb = int(input())\nprint(a+b)\nprint(a*b)',
    'Удвоить число': 'print(int(input())*2)',
    'Свой разделитель': 'first = input()\nsecond = input()\nthird = input()\nseparator = input()\nprint(first, second, third, sep=separator)',
    'Возраст в будущем': 'age = int(input())\nyears = int(input())\nprint(f"Через {years} лет вам будет {age+years} лет.")',
    'Карточка пользователя': labelled('Имя:', 'Город:', 'Возраст:'),
    'Письмо с подписью': labelled('Имя:', 'Город:'),
    'Адрес на конверте': labelled('Кому:', 'Улица:', 'Город:'),
    'Команда и аргумент': labelled('Команда:', 'Аргумент:'),
    'Имя файла': 'filename = input()\nextension = input()\nprint(f"Файл: {filename}.{extension}")',
    'Чек-лист': labelled('1.', '2.', '3.'),
  },
  102: {
    'Электронная визитка': 'name = input()\nsurname = input()\nrole = input()\nprint(f"{surname} {name} - {role}")',
    'Адрес из частей': 'city = input()\nstreet = input()\nhouse = input()\nprint(f"{city}, улица {street}, дом {house}")',
    'Копия значения': 'original = input()\nbackup = original\noriginal += "!"\nprint(original)\nprint(backup)',
    'Стоимость покупки': 'price = int(input())\ncount = int(input())\nprint(price*count)',
    'Число из строки': 'a = int(input())\nb = int(input())\nprint(a+b)',
    'Дробный результат': 'a = float(input())\nb = float(input())\nprint((a+b)/2)',
    'Пополнение копилки': 'balance = 0\nfor i in range(3):\n    balance += int(input())\nprint("Всего в копилке:", balance)',
    'Прогресс после двух шагов': 'progress = int(input())\nfor i in range(2):\n    progress += int(input())\n    print(progress)',
    'Остаток ресурса': 'stock = int(input())\nstock -= int(input())\nstock -= int(input())\nprint(stock)',
    'Герой и злодей': 'hero = input()\nvillain = input()\nprint(f"{villain} победил персонажа {hero}.")',
    'Поменять местами': swap,
    'Циклический обмен': 'a = input()\nb = input()\nc = input()\na, b, c = b, c, a\nprint(a)\nprint(b)\nprint(c)',
    'Снимок настроек': labelled('Тема:', 'Язык:', 'Уведомления:'),
    'Карточка заказа': labelled('Товар:', 'Количество:', 'Цена:'),
    'Перенос значения': 'a = input()\nb = input()\nc = input()\nprint(c)\nprint(a)\nprint(b)',
    'Профиль пользователя': 'name = input()\ncity = input()\nstatus = input()\nprint(f"{name} @{city} [{status}]")',
  },
  103: {
    'Проверка возраста': 'print("Доступ разрешен" if int(input()) >= 18 else "Доступ запрещен")',
    'Знак числа': 'n = int(input())\nprint("положительное" if n > 0 else "отрицательное" if n < 0 else "ноль")',
    'Сравнить два числа': 'a = int(input())\nb = int(input())\nprint("Первое больше" if a > b else "Второе больше" if a < b else "Числа равны")',
    'Оценка результата': 'n = int(input())\nprint("Отлично" if n >= 90 else "Хорошо" if n >= 70 else "Удовлетворительно" if n >= 50 else "Неудовлетворительно")',
    'Сигнал светофора': 'color = input().lower()\nprint("Стой" if color == "красный" else "Жди" if color in ("желтый", "жёлтый") else "Иди" if color in ("зеленый", "зелёный") else "Непонятный сигнал")',
    'Команда меню': 'print({"start":"Запуск", "pause":"Пауза", "stop":"Остановка"}.get(input().lower(), "Неизвестная команда"))',
    'Число в диапазоне': 'x = int(input())\na = int(input())\nb = int(input())\nprint("да" if a <= x <= b else "нет")',
    'Положительное чётное': 'n = int(input())\nprint("да" if n > 0 and n % 2 == 0 else "нет")',
    'Выходной день': 'print("выходной" if input().lower() in ("сб", "вс") else "будний")',
    'Логин и пароль': 'login = input()\npassword = input()\nprint("Вход выполнен" if login == "admin" and password == "qwerty" else "Ошибка доступа")',
    'Строгий пароль': 'print("Вход выполнен" if input() == "Python2026" else "Ошибка доступа")',
    'Подтверждение действия': 'print("Подтверждено" if input().lower() in ("да", "yes", "y") else "Отменено")',
    'Служебный префикс': 's = input()\nprint("служебная" if s.startswith("admin:") else "пользовательская" if s.startswith("user:") else "неизвестная")',
    'Наибольшее из трёх': 'a = int(input())\nb = int(input())\nc = int(input())\nif b > a:\n    a = b\nif c > a:\n    a = c\nprint(a)',
    'Проверка времени': 'h = int(input())\nm = int(input())\nprint("корректно" if 0 <= h <= 23 and 0 <= m <= 59 else "ошибка")',
    'Проверка имени пользователя': 's = input()\nprint("принято" if 3 <= len(s) <= 12 and s.isalnum() else "ошибка")',
    'Доступ по роли': 'print({"admin":"полный доступ", "user":"обычный доступ", "guest":"только просмотр"}[input()])',
    'Рабочий или выходной': 'print("будний" if int(input()) <= 5 else "выходной")',
    'Длина сообщения': 'print("подходит" if len(input()) >= 10 else "короткое")',
    'Статус выполнения': 'p = int(input())\nprint("не начато" if p == 0 else "готово" if p == 100 else "в работе")',
  },
  104: {
    'Сумма двух чисел': 'print(int(input()) + int(input()))',
    'Порядок действий': 'a = int(input())\nb = int(input())\nc = int(input())\nprint((a+b)*c)\nprint(a+b*c)',
    'Целая и дробная части': 'x = float(input())\nprint(int(x))\nprint(round(x-int(x), 2))',
    'Минуты в часы': 'n = int(input())\nprint(f"{n//60} ч. {n%60} мин.")',
    'Частное и остаток': 'a = int(input())\nb = int(input())\nprint(a//b)\nprint(a%b)',
    'Секунды в формат времени': 'n = int(input())\nprint(n//3600, n//60%60, n%60)',
    'Последние две цифры': 'n = int(input())\nprint(n//10%10, n%10)',
    'Округление результата': 'a = float(input())\nb = float(input())\nprint(round((a+b)/2, 2))',
    'Денежный формат': 'price = float(input())\ncount = int(input())\nprint(f"{price*count:.2f}")',
    'Изменение на процент': 'value = float(input())\np = float(input())\nprint(f"{value*(1+p/100):.2f}")',
    'Обмен значений': swap,
    'Обновление счётчика': 'n = int(input())\nfor i in range(3):\n    n += int(input())\nprint(n)',
    'Баланс после операций': 'n = int(input())\nn += int(input())\nn -= int(input())\nprint(n)',
    'Расчёт по формуле': 'd = float(input())\nc = float(input())\np = float(input())\nprint(f"{d/100*c*p:.2f}")',
    'Взвешенный результат': 'a = float(input())\nb = float(input())\nw = int(input())\nprint(f"{(a*w+b*(100-w))/100:.2f}")',
    'Номер страницы и позиция': 'n = int(input())\nk = int(input())\nprint((n-1)//k+1, (n-1)%k+1)',
    'Смещение точки': 'x = int(input())\ny = int(input())\ndx = int(input())\ndy = int(input())\nprint(x+dx, y+dy)',
    'Разделить поровну': 'n = int(input())\nk = int(input())\nprint("Каждому:", n//k)\nprint("Осталось:", n%k)',
    'Перевод байтов': 'n = int(input())\nprint("Килобайты:", n//1024)\nprint("Остаток:", n%1024)',
    'Цена со скидкой': 'price = float(input())\np = float(input())\nprint(f"Итог: {price*(1-p/100):.2f}")',
  },
  105: {
    'Повторить сообщение': 's = input()\nn = int(input())\nfor i in range(n):\n    print(s)',
    'Диапазон включительно': 'a = int(input())\nb = int(input())\nprint(*range(a,b+1))',
    'Чётные на отрезке': 'a = int(input())\nb = int(input())\nprint(*[x for x in range(a,b+1) if x%2==0])',
    'Обратный отсчёт с шагом': 'a = int(input())\nb = int(input())\nk = int(input())\nprint(*range(a,b-1,-k))',
    'Сумма введённых чисел': 'n = int(input())\nprint(sum(int(input()) for i in range(n)))',
    'Общая длина строк': 'n = int(input())\nprint(sum(len(input()) for i in range(n)))',
    'Маршрут из команд': 'n = int(input())\nprint(" -> ".join(input() for i in range(n)))',
    'Сколько раз встретился символ': 's = input()\nc = input()\ncount = 0\nfor ch in s:\n    if ch == c:\n        count += 1\nprint(count)',
    'Сколько значений равны цели': 'n = int(input())\nx = int(input())\nprint(sum(int(input()) == x for i in range(n)))',
    'Положительные, отрицательные и нули': 'n = int(input())\na = [int(input()) for i in range(n)]\nprint(sum(x>0 for x in a), sum(x<0 for x in a), a.count(0))',
    'Статистика чётных чисел': 'n = int(input())\na = [int(input()) for i in range(n)]\nb = [x for x in a if x%2==0]\nprint(len(b),sum(b))',
    'Первый максимум': 'n = int(input())\nbest = int(input())\npos = 1\nfor i in range(2,n+1):\n    x = int(input())\n    if x > best:\n        best, pos = x, i\nprint(best,pos)',
    'Самая длинная положительная серия': 'n = int(input())\ncurrent = best = 0\nfor i in range(n):\n    x = int(input())\n    current = current + 1 if x>0 else 0\n    best = max(best,current)\nprint(best)',
    'Первое вхождение': 'n = int(input())\ntarget = input()\nfound = 0\nfor i in range(1,n+1):\n    s = input()\n    if s == target and found == 0:\n        found = i\nprint(found)',
    'Порядок не нарушен': 'n = int(input())\na = [int(input()) for i in range(n)]\nprint("YES" if all(a[i] <= a[i+1] for i in range(n-1)) else "NO")',
    'Лестница из символов': 'n = int(input())\nc = input()\nfor i in range(1,n+1):\n    for j in range(i):\n        print(c,end="")\n    print()',
    'Прямоугольник из символов': 'r = int(input())\nc = int(input())\ns = input()\nfor i in range(r):\n    for j in range(c):\n        print(s,end="")\n    print()',
    'Шахматное поле': 'r = int(input())\nc = int(input())\nfor i in range(r):\n    print("".join("#" if (i+j)%2==0 else "." for j in range(c)))',
    'Собрать строку': 'n = int(input())\nprint("".join(input() for i in range(n)))',
    'Длины слов': 'n = int(input())\nprint(*[len(input()) for i in range(n)])',
    'Фильтр по длине': 'n = int(input())\nprint(sum(len(input()) >= 5 for i in range(n)))',
    'Самое длинное слово': 'n = int(input())\nbest = ""\nfor i in range(n):\n    s = input()\n    if len(s)>len(best):\n        best=s\nprint(best)',
  },
  106: {
    'Первый, последний и длина': 's = input()\nprint(len(s))\nprint(s[0])\nprint(s[-1])',
    'Строка без границ': 'print(input()[1:-1])',
    'Разворот строки': 'print(input()[::-1])',
    'Символы через один': 'print(input()[::2])',
    'Два регистра': 's = input()\nprint(s.lower())\nprint(s.upper())',
    'Замена фрагмента': 's = input()\nold = input()\nnew = input()\nprint(s.replace(old,new))',
    'Позиция фрагмента': 's = input()\nfragment = input()\nprint(s.find(fragment))',
    'Очистка краёв': 's = input().strip()\nprint(s)\nprint(len(s))',
    'Строка из цифр': 'print("да" if input().isdigit() else "нет")',
    'Корректное имя': 'name = input()\nprint(f"Привет, {name}!" if name.isalpha() else "Некорректное имя")',
    'Тип файла': 's = input().lower()\nprint("Python" if s.endswith(".py") else "Изображение" if s.endswith((".jpg",".png")) else "Другой")',
    'Количество слов': 'print(len(input().split()))',
    'Сколько раз встретился символ': 's = input().lower()\nc = input().lower()\nprint(s.count(c))',
    'Количество гласных': 'print(sum(c in "ауоыиэяюёе" for c in input().lower()))',
    'Чередование регистра': 's = input()\nprint("".join(c.lower() if i%2==0 else c.upper() for i,c in enumerate(s)))',
    'Удвоение символов': 'print("".join(c*2 for c in input()))',
    'Оставить только цифры': 'print("".join(c for c in input() if c.isdigit()))',
    'Убрать соседние повторы': 's = input()\nprint("".join(c for i,c in enumerate(s) if i==0 or c!=s[i-1]))',
    'Убрать гласные': 'print("".join(c for c in input() if c.lower() not in "ауоыиэяюёеaeiou"))',
    'Палиндром': 's = input().replace(" ", "").lower()\nprint("да" if s==s[::-1] else "нет")',
    'Сжать пробелы': 'print(" ".join(input().split()))',
    'Соседние пары': 'print("-".join(input()))',
  },
  107: {
    'Эхо до стоп-слова': 's = input()\nwhile s.lower() != "стоп":\n    print(s)\n    s = input()',
    'Сумма до нуля': 'total = 0\nn = int(input())\nwhile n != 0:\n    total += n\n    n = int(input())\nprint(total)',
    'Пароль до успеха': 'while input() != "secret":\n    print("Неверно")\nprint("Доступ разрешен")',
    'Числа от 1 до N': 'n = int(input())\ni = 1\na = []\nwhile i <= n:\n    a.append(i)\n    i += 1\nprint(*a)',
    'Шаг до границы': 'a = int(input())\nb = int(input())\nk = int(input())\nres = []\nwhile a <= b:\n    res.append(a)\n    a += k\nprint(*res)',
    'Обратный отсчёт': 'n = int(input())\na = []\nwhile n >= 0:\n    a.append(n)\n    n -= 1\nprint(*a)',
    'Сумма цифр': 'n = int(input())\ns = 0\nwhile n:\n    s += n%10\n    n //= 10\nprint(s)',
    'Количество цифр': 'n = int(input())\nc = 1\nwhile n >= 10:\n    n //= 10\n    c += 1\nprint(c)',
    'Разворот числа': 'n = int(input())\nr = 0\nwhile n:\n    r = r*10+n%10\n    n //= 10\nprint(r)',
    'Наибольшая цифра': 'n = int(input())\nbest = 0\nwhile n:\n    best = max(best,n%10)\n    n //= 10\nprint(best)',
    'Следующая степень двойки': 'n = int(input())\np = 1\nwhile p <= n:\n    p *= 2\nprint(p)',
    'Шаги до цели': 'a = int(input())\nb = int(input())\nk = int(input())\nc = 0\nwhile a < b:\n    a += k\n    c += 1\nprint(c)',
    'Рост до границы': 'a = int(input())\nb = int(input())\nc = 0\nwhile a < b:\n    a *= 2\n    c += 1\nprint(c)',
    'Первое допустимое значение': 'n = int(input())\nwhile n < 0:\n    n = int(input())\nprint(n)',
    'Поиск символа вручную': 's = input()\nc = input()\ni = 0\nwhile i < len(s) and s[i] != c:\n    i += 1\nprint(i if i < len(s) else -1)',
    'Убрать ведущие нули': 's = input()\ni = 0\nwhile i < len(s)-1 and s[i] == "0":\n    i += 1\nprint(s[i:])',
  },
  108: {
    'Первый и последний элемент': 'a = list(map(int,input().split()))\nprint(a[0], a[-1])',
    'Сводка списка': 'a = list(map(int,input().split()))\nprint(sum(a))\nprint(min(a))\nprint(max(a))',
    'Количество заданного значения': 'a = list(map(int,input().split()))\nx = int(input())\nprint(a.count(x))',
    'Только чётные': 'print(*[x for x in map(int,input().split()) if x%2==0])',
    'Замена отрицательных': 'print(*[-1 if x<0 else x for x in map(int,input().split())])',
    'Удвоить каждый элемент': 'print(*[x*2 for x in map(int,input().split())])',
    'Обратный порядок': 'print(*input().split()[::-1])',
    'Сортировка по возрастанию': 'a = list(map(int,input().split()))\na.sort()\nprint(*a)',
    'Объединение через решётку': 'print("#".join(input().split()))',
    'Уникальные по порядку': 'a = list(map(int,input().split()))\nres = []\nfor x in a:\n    if x not in res:\n        res.append(x)\nprint(*res)',
    'Сдвиг вправо': 'a = list(map(int,input().split()))\nprint(*[a[-1]],*a[:-1])',
    'Больше предыдущего': 'a = list(map(int,input().split()))\nprint(*[a[i] for i in range(1,len(a)) if a[i]>a[i-1]])',
    'Локальные максимумы': 'a = list(map(int,input().split()))\nprint(*[a[i] for i in range(1,len(a)-1) if a[i]>a[i-1] and a[i]>a[i+1]])',
    'Разности соседей': 'a = list(map(int,input().split()))\nprint(*[a[i]-a[i-1] for i in range(1,len(a))])',
    'Длина возрастающей серии': 'a = list(map(int,input().split()))\ncurrent = best = 1\nfor i in range(1,len(a)):\n    current = current+1 if a[i]>a[i-1] else 1\n    best = max(best,current)\nprint(best)',
    'Поменять минимум и максимум': 'a = list(map(int,input().split()))\ni = a.index(min(a))\nj = a.index(max(a))\na[i],a[j] = a[j],a[i]\nprint(*a)',
    'Первая и последняя позиция': 'a = list(map(int,input().split()))\nx = int(input())\nprint(a.index(x),len(a)-1-a[::-1].index(x))',
    'Вставка по индексу': 'a = list(map(int,input().split()))\ni = int(input())\nx = int(input())\na.insert(i,x)\nprint(*a)',
  },
  109: {
    1770026857230: 't = tuple(input().split())\nprint(t[0])\nprint(t[-1])',
    1770026875192: 't = tuple(map(int,input().split()))\na = list(t)\na.pop()\nprint(tuple(a))',
    1770026891328: 'x,y,z = tuple(map(int,input().split()))\nprint(x+z-y)',
    1770026901750: 'print(tuple(input())[1:-1:2])',
    1770026916764: 't = tuple(map(int,input().split()))\nn = int(input())\nprint(t.count(n))\nprint(t.index(n))',
  },
  110: {
    1770027189357: 'def calculate_price(price,discount):\n    return price*(1-discount/100)\nprint(calculate_price(int(input()),int(input())))',
    1770027201191: 'def get_max(a,b,c):\n    if b>a:\n        a=b\n    if c>a:\n        a=c\n    return a\nprint(get_max(int(input()),int(input()),int(input())))',
    1770027213652: 'def fact(n):\n    return 1 if n==0 else n*fact(n-1)\nprint(fact(int(input())))',
    1770027229293: 'def fib(n):\n    return 1 if n<=2 else fib(n-1)+fib(n-2)\nprint(fib(int(input())))',
    1770027247190: 'def print_reversed(n):\n    print(n%10,end="")\n    if n//10:\n        print_reversed(n//10)\nprint_reversed(int(input()))',
  },
  112: { 1789807484135: 'print(input())' },
  217: {
    'Суммы соседних пар': 'for i in range(len(a)-1):\n    print(a[i]+a[i+1])',
    'Максимальная сумма среди соседних пар': 's = [a[i]+a[i+1] for i in range(len(a)-1)]\nprint(max(s),s.count(max(s)))',
    'Сколько пар строго возрастающих': 'print(sum(a[i]<a[i+1] for i in range(len(a)-1)))',
    'Сколько смен знака между соседями': 'print(sum(a[i]*a[i+1]<0 for i in range(len(a)-1)))',
    'Сколько локальных максимумов': 'print(sum(a[i]>a[i-1] and a[i]>a[i+1] for i in range(1,len(a)-1)))',
    'Первый индекс тройки строго возрастающей': 'found = -1\nfor i in range(len(a)-2):\n    if a[i]<a[i+1]<a[i+2]:\n        found = i\n        break\nprint(found)',
    'Минимальная абсолютная разница соседей': 'print(min(abs(a[i]-a[i+1]) for i in range(len(a)-1)))',
    'Циклическая максимальная соседняя сумма': 'print(max(a[i]+a[(i+1)%len(a)] for i in range(len(a))))',
    'Квадраты всех чисел': 'for x in a:\n    print(x*x)',
    'Только чётные': 'for x in a:\n    if x%2==0:\n        print(x)',
    'Модули чисел': 'for x in a:\n    print(abs(x))',
    'Кратные 7, но не кратные 14': 'for x in a:\n    if x%7==0 and x%14!=0:\n        print(x)',
    'Индексы положительных': 'for i,x in enumerate(a):\n    if x>0:\n        print(i)',
    'Разности соседей (окно 2)': 'for i in range(len(a)-1):\n    print(a[i+1]-a[i])',
    'Суммы трёх подряд': 'for i in range(len(a)-2):\n    print(sum(a[i:i+3]))',
    'Соседние пары': 'for i in range(len(a)-1):\n    print(a[i],a[i+1])',
    'Квадраты только нечётных': 'for x in a:\n    if x%2:\n        print(x*x)',
    'Сделать минимальное число “нулём”': 'mn = min(a)\nfor x in a:\n    print(x-mn)',
  },
  225: {
    'Найди числа, которые делятся на 3': 'a,b = map(int,input().split())\nres = [x for x in range(a,b+1) if x%3==0]\nprint(" ".join(map(str,res)) if res else "NO")',
    'Первые 5 чётных чисел, начиная с N': 'n = int(input())\nstart = n+n%2\nprint(*range(start,start+10,2))',
    'Сколько чисел оканчиваются на 7': 'a,b = map(int,input().split())\nprint(sum(abs(x)%10==7 for x in range(a,b+1)))',
    'Все делители числа': 'n = int(input())\nprint(*[d for d in range(1,n+1) if n%d==0])',
    'Сколько у числа делителей': 'n = int(input())\nprint(sum(n%d==0 for d in range(1,n+1)))',
    'Найди самый большой делитель, кроме самого числа': 'n = int(input())\nprint(max(d for d in range(1,n) if n%d==0))',
  },
};

export function referenceFor(topic, question) {
  const code = references[topic]?.[question.id] ?? references[topic]?.[question.title];
  if (!code) return null;
  return String(topic) === '217' ? 'import sys\na = list(map(int,sys.stdin.read().split()))\n' + code : code;
}
